<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xcky.mapper.NoticeTypeInfoMapper">
    <insert id="insertNoticeTypeInfo" parameterType="com.xcky.model.entity.NoticeTypeInfo">
        INSERT INTO
            notice_type_info
        (
            type_name,
            create_time,
            update_time,
            status,
            cron_config,
            excute_url,
            notice_template
        )
        VALUES
        (
            #{typeName},
            #{createTime},
            #{updateTime},
            #{status},
            #{cronConfig},
            #{excuteUrl},
            #{noticeTemplate}
        )
    </insert>

    <update id="updateNoticeTypeInfo" parameterType="com.xcky.model.entity.NoticeTypeInfo">
        UPDATE
            notice_type_info
        SET
            type_name = #{typeName},
            update_time = #{updateTime},
            status = #{status},
            cron_config = #{cronConfig},
            excute_url = #{excuteUrl},
            notice_template = #{noticeTemplate}
        WHERE
            id = #{id}
    </update>
    
    <select id="seleteNoticeTypeByMap" parameterType="java.util.Map" resultType="com.xcky.model.entity.NoticeTypeInfo">
        SELECT
            id,
            type_name typeName,
            create_time createTime,
            update_time updateTime,
            status,
            cron_config cronConfig,
            excute_url excuteUrl,
            notice_template noticeTemplate
        FROM
            notice_type_info
        WHERE
            1=1
            <if test="null != id">
                AND id = #{id}
            </if>
            <if test="null != status and status != ''">
                AND status = #{status}
            </if>
            <if test="null != typeName and typeName != ''">
                AND type_name LIKE CONCAT('%',#{typeName},'%');
            </if>
    </select>

    <delete id="deleteNoticeTypeByIds" parameterType="java.util.List">
        UPDATE
          notice_type_info
        SET
          status = 'N'
        WHERE
          1=1
          AND ID IN
          <foreach collection="list" item="item" separator="," open="(" close=")">
              #{item}
          </foreach>
    </delete>
</mapper>
