<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xcky.mapper.EmailSendInfoMapper">
    <insert id="insertEmailSendInfo" parameterType="com.xcky.model.entity.EmailSendInfo">
        INSERT INTO
          email_send_info
        (
            from_email
            to_email
            subject
            email_content
            create_time
            update_time
            record_date
        )
        VALUES
        (
            #{fromEmail},
            #{toEmail},
            #{subject},
            #{emailContent},
            #{createTime},
            #{createTime},
            #{createTime}
        )
    </insert>

</mapper>
